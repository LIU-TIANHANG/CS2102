<!DOCTYPE html>
<html>
<head>
    <% include ../partials/home %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.7/dist/css/bootstrap-select.min.css">

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.7/dist/js/bootstrap-select.min.js"></script>

    <!-- (Optional) Latest compiled and minified JavaScript translation files -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.7/dist/js/i18n/defaults-*.min.js"></script>

    <style>
        .container{
            overflow: scroll;
        }
        .row{
            padding: 1%;
        }
        .jumbotron{
            width: 100%;
            height: 350px;
            background-image: url(https://images.pexels.com/photos/958545/pexels-photo-958545.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940);
            background-size:cover;
            background-repeat: no-repeat;
            opacity: 0.9;
            text-align: center;
        }
        .masthead-brand {
            margin-top: 35px;
            font-style: oblique;
        }
        .main {
            overflow: scroll;
            height:fit-content;
        }
        .btn {
            background-color: transparent;
            border-color: darkgoldenrod;
            color: #271E1E;
            font-weight: bolder;
        }
        .container-head{
            height:fit-content;
            background-color: #271E1E;
            align-content: center;
        }
        .row{
            vertical-align: -webkit-baseline-middle;
        }
    </style>
</head>
<body>
<div class="main">
    <div class="jumbotron">
        <div class="card" style="background-color: blanchedalmond; opacity: 0.70; margin-top: 5vh">
            <h1 class="masthead-brand mb-3 mt-5">View Restaurants</h1>
            <div class="row">
                <div class="col"><hr></div>
                <div class="col-auto">Displays search results based on filter selections</div>
                <div class="col"><hr></div>
            </div>
        </div>
    </div>

    <div class="container">
            
        <div class="row">
            <div class="col">
                <div class="d-flex justify-content-end mb-3">
                        <div class="btn-toolbar">
                                <button type="reset" class="btn btn-primary btn-lg mt-3 mr-1" role="button" onclick="history.back(-1)" aria-pressed="true">Back</button>
                                <a href="/redirect" class="btn btn-primary btn-lg mt-3" role="button" aria-pressed="true" style="background-color: gold">Return to Index</a>
                            </div>
                    </div>
                <br>
                <h4>View All Restaurants</h4>
                <table class="table">
                    <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Opening Hours</th>
                        <th scope="col">Intro</th>
                        <th scope="col">Contact</th>
                        <th scope="col">Meal Types</th>
                        <th scope="col">Cuisine</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% for(var i=0; i<data.length; i++) { %>
                    <tr>
                        <td><%= data[i].rname%></td>
                        <td><%= data[i].openinghour%></td>
                        <td><%= data[i].intro%></td>
                        <td><%= data[i].contactno%></td>
                        <td><%= data[i].mealtype%></td>
                            <td><%= data[i].cuisine%></td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

</body>

</html>